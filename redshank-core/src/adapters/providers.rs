//! LLM provider adapters.

// TODO(T06): anthropic.rs — AnthropicModel implements ModelProvider
// TODO(T07): openai_compat.rs — OpenAICompatibleModel implements ModelProvider
// TODO(T08): builder.rs — build_provider() factory
