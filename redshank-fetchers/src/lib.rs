//! # redshank-fetchers
//!
//! Public data fetcher binaries and shared infrastructure for the
//! Redshank investigation agent.

pub mod domain;

#[cfg(test)]
mod tests {
    #[test]
    fn it_compiles() {
        assert!(true);
    }
}
